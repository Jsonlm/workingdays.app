services:
  - type: web
    name: working-days-api
    env: node
    plan: free
    nodeVersion: 20.19.4
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
